apiVersion: v1
kind: Service
metadata:
  name: esp-srv
  labels:
    type: esp-srv
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 80
  selector:
    app: taxisbahiadeploy
